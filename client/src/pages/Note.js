import React from 'react';
import { useParams } from 'react-router-dom';

const Note = () => {
  // const { item } = props.location.state;
  // console.log(item)

  // Use the 'item' value in your component

  return (
    <div>
      <h1>Note </h1>
      {/* <p>Item: {item}</p> */}
    </div>
  );
};

export default Note;
